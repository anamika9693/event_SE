<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>CodePen - Feedback Form</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/mavo/0.2.0/mavo.min.css'>
    <link rel="stylesheet" href="../public/feedback/event_feedback.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prefixfree/1.0.7/prefixfree.min.js"></script>

</head>

<body>
    <!-- partial:index.partial.html -->
    <!-- Credit: Form Design is inspired by Aswin Madhulal (https://dribbble.com/shots/7209761-Feedback-much-appreciated) -->
    <div class="content">
        <header>
            <h1>Feel free to drop us your feedback.</h1>
            <!-- 	Credit: https://undraw.co/ -->
            <img src="https://s3-us-west-2.amazonaws.com/s.cdpn.io/1457119/feedback-form-template.svg"
                alt="A woman is sitting on the floor and working on a laptop (vector illustration)" />
        </header>
        <main mv-app="feedbackForm" mv-mode="read">
            <form method='post' action='/given_feedback'>
                <label>
                    <h2>
                        Your Email:
                    </h2>
                </label>
                <input type='text' property='comment' name='email' style='height:1em;overflow:hidden;' value='{{email}}'
                    readonly>
                <input type='hidden' name='event' value='{{event}}'>
                <label>
                    <h2>
                        How satisfied are you overall with the event?
                    </h2>

                    <div class="tickmarks">
                        <span mv-multiple="tickmark" mv-value="0 .. 10" mv-storage="none"
                            class="[if($this = score, highlight)]" mv-action="set(score, $this)"></span>
                    </div>

                    <input property="score" type="range" max="10" name='score' style="--score: [score * 10]%" />

                    <div class="legend">
                        <span mv-action="set(score, 0)">
                            Not at all likely
                        </span>
                        <span mv-action="set(score, 10)">
                            Extremely likely
                        </span>
                    </div>
                </label>

                <textarea property="comment" cols="30" rows="10"
                    placeholder="Please tell us your reasons for giving this score here..." style="overflow-y:scroll;"
                    name='comment'></textarea>

                <button class="mv-save" type='submit'>
                    Send Feedback
                </button>
            </form>
        </main>
    </div>

    <!-- We need an empty source (which is different from the storage) for not to show end-users previously-stored feedback of other users. -->
    <!-- partial -->
    <script src='https://cdnjs.cloudflare.com/ajax/libs/mavo/0.2.0/mavo.es5.js'></script>
</body>

</html>